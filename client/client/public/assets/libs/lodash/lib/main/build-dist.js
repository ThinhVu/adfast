"use strict";function build(){async.series([file.copy(baseLodash,distLodash),file.min(distLodash)],util.pitch)}const async=require("async"),path=require("path"),file=require("../common/file"),util=require("../common/util"),basePath=path.join(__dirname,"..",".."),distPath=path.join(basePath,"dist"),filename="lodash.js",baseLodash=path.join(basePath,filename),distLodash=path.join(distPath,filename);build();